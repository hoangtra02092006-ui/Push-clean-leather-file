{
  "name": "printnest-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "type-check": "vue-tsc --noEmit",
    "test": "esbuild src/api/units.ts --bundle --format=esm --platform=node --outfile=dist-test/units.mjs --log-level=error && esbuild src/api/mock.ts --bundle --format=esm --platform=node --outfile=dist-test/mock.mjs --log-level=error && node test/units.test.mjs && node test/mock.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "pinia": "^2.2.4",
    "vue": "^3.5.12",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.9",
    "vue-tsc": "^2.1.6"
  }
}
